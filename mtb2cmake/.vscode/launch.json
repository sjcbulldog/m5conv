{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "MTB->CMake",
            "type": "node",
            "request": "launch",
            "program": "${workspaceFolder}/src/index.ts",
            "preLaunchTask": "npm: build",
            "outFiles": ["${workspaceFolder}/dist/**/*.js"],
            "sourceMaps": true,
            "console": "integratedTerminal",
            "args": [
                "--force",
                "--depends", "${workspaceFolder}/depends.json",
                "--target", "gcc",
                "--source", "c:/sdk/mtb/KIT_PSE84_EVAL_EPC2/${input:mtbProjectName}",
                "--dest", "c:/sdk/mtb5/${input:mtbProjectName}",
            ]
        }
    ],
    "inputs": [
        {
            "id" : "mtbProjectName",
            "type" : "promptString",
            "description" : "Name of the project to convert",
            "default" : "graphics"
        }
    ]
}
